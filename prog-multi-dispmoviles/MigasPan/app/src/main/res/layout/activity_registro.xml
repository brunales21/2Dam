
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#3498db"
    android:padding="16dp">

    <EditText
        android:id="@+id/editTextUsuario"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Usuario"
        android:textColor="#000000"
        android:background="#ffffff"
        android:layout_marginBottom="8dp"/>

    <EditText
        android:id="@+id/editTextContraseña"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Contraseña"
        android:inputType="textPassword"
        android:textColor="#000000"
        android:background="#ffffff"
        android:layout_below="@id/editTextUsuario"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp"/>

    <EditText
        android:id="@+id/editTextRepiteContraseña"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Repite Contraseña"
        android:inputType="textPassword"
        android:textColor="#000000"
        android:background="#ffffff"
        android:layout_below="@id/editTextContraseña"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp"/>

    <Spinner
        android:id="@+id/spinnerNivel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/editTextRepiteContraseña"
        android:layout_marginTop="8dp"
        android:textColor="#000000"
        android:background="#ffffff"/>

    <Button
        android:id="@+id/btnAceptarRegistro"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/spinnerNivel"
        android:layout_marginTop="72dp"
        android:background="#2ecc71"
        android:text="Aceptar"
        android:textColor="#ffffff" />
</RelativeLayout>
